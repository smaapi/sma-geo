---
import pagesData from '../data/pages.json';
import { organization } from '../lib/schema';

interface Props {
  route: string;
}

const { route } = Astro.props;
const site = pagesData.site;
const page = pagesData.pages.find((p) => p.route === route);
if (!page) throw new Error(`route not in src/data/pages.json registry: ${route}`);
const alt = pagesData.pages.find((p) => p.route === page.alternate);
const zh = page.lang === 'zh';
const canonical = site + route;
const xDefault = zh ? (alt ? site + alt.route : canonical) : canonical;
const nav = zh
  ? [
      { href: '/', label: '首页' },
      { href: '/zh/what-is-llm-gateway', label: '什么是 LLM 网关' },
      { href: '/zh/faq', label: 'FAQ' },
    ]
  : [
      { href: '/en/', label: 'Home' },
      { href: '/en/what-is-llm-gateway', label: 'What is an LLM gateway' },
      { href: '/en/faq', label: 'FAQ' },
    ];
---

<!doctype html>
<html lang={zh ? 'zh-CN' : 'en'}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{page.title}</title>
    <meta name="description" content={page.description} />
    <link rel="canonical" href={canonical} />
    <link rel="alternate" hreflang={zh ? 'zh-CN' : 'en'} href={canonical} />
    {alt && <link rel="alternate" hreflang={zh ? 'en' : 'zh-CN'} href={site + alt.route} />}
    <link rel="alternate" hreflang="x-default" href={xDefault} />
    <meta property="og:title" content={page.title} />
    <meta property="og:description" content={page.description} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonical} />
    <meta property="og:locale" content={zh ? 'zh_CN' : 'en_US'} />
    <meta name="twitter:card" content="summary" />
    <script type="application/ld+json" set:html={JSON.stringify(organization)} />
    <slot name="jsonld" />
  </head>
  <body>
    <header>
      <nav>
        <span class="brand">SMA</span>
        {nav.map((item) => <a href={item.href}>{item.label}</a>)}
        {alt && <a class="lang-switch" href={alt.route}>{zh ? 'English' : '中文'}</a>}
      </nav>
    </header>
    <main>
      <slot />
    </main>
    <footer>
      <p>
        {zh ? '更新于' : 'Last updated'} {page.dateModified} · © 2026 {zh ? '菌路科技' : 'Slime Mould Tech'}
      </p>
    </footer>
  </body>
</html>

<style is:global>
  :root {
    --fg: #1a202c;
    --muted: #4a5568;
    --line: #e2e8f0;
    --accent: #2b6cb0;
  }
  * {
    box-sizing: border-box;
  }
  body {
    margin: 0;
    color: var(--fg);
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC', 'Hiragino Sans GB',
      'Microsoft YaHei', sans-serif;
    line-height: 1.7;
  }
  header nav,
  main,
  footer p {
    max-width: 760px;
    margin: 0 auto;
    padding: 0 1.25rem;
  }
  header {
    border-bottom: 1px solid var(--line);
  }
  header nav {
    display: flex;
    gap: 1rem;
    align-items: center;
    padding-top: 0.75rem;
    padding-bottom: 0.75rem;
    flex-wrap: wrap;
  }
  .brand {
    font-weight: 700;
  }
  .lang-switch {
    margin-left: auto;
  }
  a {
    color: var(--accent);
    text-decoration: none;
  }
  a:hover {
    text-decoration: underline;
  }
  main {
    padding-top: 1.5rem;
    padding-bottom: 3rem;
  }
  h1 {
    line-height: 1.3;
  }
  .answer {
    border-left: 4px solid var(--accent);
    background: #f7fafc;
    padding: 0.75rem 1rem;
    margin: 1rem 0 2rem;
    font-size: 1.05rem;
  }
  table {
    border-collapse: collapse;
    width: 100%;
    margin: 1rem 0;
  }
  th,
  td {
    border: 1px solid var(--line);
    padding: 0.5rem 0.75rem;
    text-align: left;
    vertical-align: top;
  }
  th {
    background: #f7fafc;
  }
  footer {
    border-top: 1px solid var(--line);
    color: var(--muted);
    font-size: 0.9rem;
  }
  footer p {
    padding-top: 1rem;
    padding-bottom: 1rem;
  }
  .related {
    margin-top: 2.5rem;
    border-top: 1px solid var(--line);
    padding-top: 1rem;
  }
</style>
